using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SelfDrivingController : MonoBehaviour {

    // create a population of neural networks, each coupled to a car

    // LOOP
        // run NNs and evaluate fitness
        
        // create a new population
        // pick parents based on fitness
        // - crossover (weights and biases)
        // - mutation

    
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
